{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "activity-indicator",
  "description": "State event access for the non-interactive activity indicator.",
  "windows": ["activity-indicator"],
  "permissions": [
    "core:event:allow-listen",
    "core:event:allow-unlisten",
    "activity-indicator-window-commands"
  ]
}
